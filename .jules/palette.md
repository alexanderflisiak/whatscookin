## 2026-04-15 - Toggle Buttons State
**Learning:** When using standard `<button>` tags as toggle switches (like the Lazy Filter mode), applying `aria-pressed={state}` ensures screen readers can announce the active vs inactive toggle state correctly.
**Action:** Always pair `aria-pressed` with visual toggle states for filter or mode buttons.
