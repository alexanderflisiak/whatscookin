## 2026-06-08 - Adding accessibility to inline form inputs
**Learning:** When form inputs (e.g., in inline rows or grids) lack explicit visible text `<label>` elements, always add descriptive `aria-label` attributes to the `<input>` elements to ensure screen reader accessibility.
**Action:** Use `aria-label` attributes on any input that lacks a descriptive visible label.
