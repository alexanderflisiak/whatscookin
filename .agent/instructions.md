For GitHub: "Before starting a new feature, create a branch named feature/[task-name]. When finished, push to GitHub and provide me the link to the PR."

For Vercel: "After pushing to GitHub, use the Vercel MCP to monitor the deployment. If the build fails, read the logs and fix the code locally before pushing a correction."

For Environment Variables: "Check the Vercel project settings via MCP to ensure the DATABASE_URL is present before confirming the deployment is 'Production Ready'.